How do I add a flag to an alias? <body> Is it possible to add a flag to a embash/em alias you create yourself? e.g.  precodecon -a = 'ssh username@server1.domain.com' con -b = 'ssh username@server2.domain.com' /code/pre  <answer51007> Nope – aliases are simple text substitutions. Use different alias names instead:  precodealias cona='ssh username@server1.domain.com'  alias conb='ssh username@server2.domain.com' /code/pre  emEDIT if absolutely must have flags, a function will serve better than an alias – see @glenn-jackmann’s answer/em.  <answer51010> Or, use a function instead of an alias:  precodecon() {   local OPTIND svr   while getopts ":ab" option; do     case $option in       a) svr=server1 ;;       b) svr=server2 ;;       ?) echo "invalid option: $OPTARG"; return 1 ;;     esac   done   ssh username@${svr}.domain.com }  con -a /code/pre  <comment58336> Right. Though, considering OP only needs a single flag, the `getopts` loop might be a teensy-weensy bit overkill :). <comment58352> If you're going to show someone how to do something, show them the correct way. <comment58354> not contesting the merits of your answer – I linked to it indicating it is better than mine. <comment58624> Can this be placed in .profile along with aliases? <comment58625> .bashrc is usually recommended