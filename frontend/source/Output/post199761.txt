Different values for du and du -h <body> On the command line, codedu/code seems to return twice the numeric value returned by codedu -h/code.   Apparently, codedu -h/code resembles the actual file size, so what's the plain codedu/code value representing?  <answer199764>    strong-h/strong "Human-readable" output.  Use unit suffixes: Byte, Kilobyte, Megabyte, Gigabyte, Terabyte and Petabyte.   If -h is not given, the sizes are given in 512-byte blocks.  <comment240549> Right, I missed the blocksize part on the man page. Thanks.