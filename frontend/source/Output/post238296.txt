Dump iCloud keychain in Terminal <body> I failed to find the path to iCloud keychain to do just codesecurity dump-keychain/code - the iCloud keychain isn't shown when I try codesecurity list-keychains/code. What is the path to that keychain (or, if it cannot be obtained, how to dump the whole iCloud keychain)?  Or, maybe, Apple Script could help to do that?  <comment292195> You only need to see it with the Terminal ? <comment292196> @Chris, actually, in Single-user mode