Finder View Options <body> In Finder's view options:  ul liI assume that "Always open in list view" pertains only to the current folder. True?/li liI assume that "Use as Defaults" pertains to the icon size, text size, and columns for that type of view—icons, list, columns, or cover flow—and is folder agnostic. True?  ul liFor example, I set the list view to use the small icon size and 10 point font. Would that propagate across all folders opened in the list view or is that specific to just the current folder?/li /ul/li /ul  img src="https://i.stack.imgur.com/e2AwA.png" alt="enter image description here"  <answer14478> "Use as Defaults" means for all windows, and in your case for all list view windows. 