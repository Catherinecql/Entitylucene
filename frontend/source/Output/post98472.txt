Alternative to cp --parents in terminal <body> I was trying to use the code--parents/code option with codecp/code (to create the non-existing directories in the target path) in the terminal but that option does not seem to be working in MacOS.    precode&gt;cp --parents src_dir trgt_dir cp: illegal option -- - usage: cp [-R [-H | -L | -P]] [-fi | -n] [-apvX] source_file target_file        cp [-R [-H | -L | -P]] [-fi | -n] [-apvX] source_file ... target_directory /code/pre  Any alternative?  <answer98483> Perhaps use codemkdir -p/code to create the path to the target directory first.  precodemkdir -p foo/bar/cheese cp src_dir foo/bar/cheese /code/pre  <answer98502> (Using codetree/code to display directories. Installed from homebrew)  precode[valbaca@AppleJack foo]\$ tree . └── a     └── b         └── c             └── d  3 directories, 1 file [valbaca@AppleJack foo]\$ cp -R a blah [valbaca@AppleJack foo]\$ tree . ├── a │   └── b │       └── c │           └── d └── blah     └── b         └── c             └── d /code/pre  <answer98506> codersync -R/code (code--relative/code) is like codecp --parents/code. So codersync -rR a/b c/code copies to codec/a/b/code instead of codec/b/code.  Or just install the GNU codecp/code with codebrew install coreutils/code or codesudo port install coreutils/code and use codegcp -r --parents a/b c/code. 