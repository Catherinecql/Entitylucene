Convert shell script order and underscores to spaces <body> This shell script:  precodefilepath=$1 dirpath=${filepath%/*} result=$(basename "$dirpath") author=${result%_*_*}  exiftool -author="$author" -P -overwrite_original_in_place "$1" /code/pre  Takes this folder name:  codeJohn_iPhone_Uploads/code  ...and adds an EXIF Author Keyword of John to each image in its folder using exiftool.  How can I convert this script to do the same with this folder name:  codeiPhone Uploads John/code 