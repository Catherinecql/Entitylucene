How do I reload an OS X "bash" user-environment <body> I think that I want to ask  ul liHow do I reload an OS X codebash/code user-environment/li /ul  But damnit Jim ; I'm a software engineer not a sysadmin  h1The Goal/h1  ul liI want Jenkins started as a Daemon on El Capitan/li liI want to use "UserWhoHasARealAccount" on OS X/li liFor this I should be using a launch-daemon/li /ul  AFAIK  ul liall strongem/Library/LaunchDaemons//em/strong are started as strongemroot/em/strong/li liWhile they can substitute-user with strongemUser/em/strong to become strongemnot-root/em/strong they still retain emroot's/em environment./li /ul  h1The Problem/h1  ul liprocess won't have the full PATH (et al) that I setup  ul licodewhich/code can work around this but .../li /ul/li lisub processes don't get the PATH I setup for the user  ul liso codewhich whatever-i-want/code doesn't help when Node.JS tries to run a subprocess and compile my blog ... unless I for Node.JS to invoke subprocesses via absolute paths on my computer/li /ul/li lithe strongemroot/em/strong environment seems to prevent codessh/code from working  ul liso I can't have have "UserWhoHasARealAccount" login to localhost to magically get a real path/li liI may be missing something since I can't find any SSH error messages/li /ul/li /ul  Since this is a server, logging in and pressing a button won't "do it for me"  <answer226697> For anyone from the future ..  codebash/code can be run with code-l/code to execute full login goodies. So, my launchd script goes from;  precode&lt;ProgramArguments&gt;     &lt;array&gt;         &lt;string&gt;java&lt;/string&gt;         &lt;string&gt;-jar&lt;/string&gt;         &lt;string&gt;jenkins.war&lt;/string&gt;     &lt;/array&gt; /code/pre  ... to ...  precode&lt;ProgramArguments&gt;     &lt;array&gt;         &lt;string&gt;bash&lt;/string&gt;         &lt;string&gt;-l&lt;/string&gt;         &lt;string&gt;-c&lt;/string&gt;         &lt;string&gt;java -jar jenkins.war&lt;/string&gt;     &lt;/array&gt; /code/pre 