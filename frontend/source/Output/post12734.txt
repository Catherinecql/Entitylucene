Mac OSX email message report <body> How can i get a message report that tells me that an email i sent has been received and opened?  <answer12737> You can't as the receivers client has to send back information and most clients do not do that, ie they can read the email without marking anything In the RFCs covering mail there is nothing that must be done to provide this functionality. You can probably tell that the mail has reached the recipient's mailbox but that is it.  <answer12739> The one way you can "sort of" get a return receipt like you're asking for is emIF/em the system Mac Mail is connecting to has the capability.  For example, you can get request receipts with MS-Exchange. If you setup Apple Mail to communicate with exchange – you probably also have access to a web-version for your email system, which will probably let you setup read receipts for the messages you send from the web client.  But no, if you're just using Apple Mail with a typical POP, IMAP or Mobile-Me setup you're not going to have access to a read receipt feature.  <answer12740> You can enable a Read Request Receipt in Apple Mail, so strongemif the receiver client support it/em/strong, you'll get a "Read Receipt" back.  It is possible to add these headers to to outgoing mails:  ul li"Disposition-Notification-To" em(Read Request Receipt)/em/li liBcc/li li"Reply-To"/li /ul  To make Apple Mail request a read receipt for every message you send:  ul liOpen Terminal./li liType "strongdefaults read com.apple.mail UserHeaders/strong"./li liPress Enter./li liIf that command returns "The domain/default pair of (com.apple.mail, UserHeaders) does not exist":  ul liType/li /ul  codedefaults write com.apple.mail UserHeaders '{"Disposition-Notification-To" = "Name &lt;email@address&gt;"; }'/code  replacing Name with your name and email@address with your email address.  ul liThe complete line could read strongdefaults write com.apple.mail UserHeaders '{"Disposition-Notification-To" = "FirstName LastName &lt;sender@example.com"; }'/strong/li /ul/li /ul  And to add one of the other headers you just add it to the line - then the line could read like this:  strongdefaults write com.apple.mail UserHeaders '{"Reply-To" = "ReplyFirstName ReplyLastName &lt;reply@example.com"; Bcc = "BccFirstName BccLastName &lt;bcc@example.com"; "Disposition-Notification-To" = "FirstName LastName &lt;sender@example.com"; }'/strong  <comment13364> Indeed, for privacy reasons many people (myself included) prefer not to allow the sending of such return receipts. If I see an incoming message late at night but decide not to answer it until the next day, I don't want the other party to be aware of that decision.