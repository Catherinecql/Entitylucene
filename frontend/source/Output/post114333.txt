BootChamp equivalent to go from Windows to Mac <body> I use BootChamp to restart into Windows directly from OS X. I was wondering if there is an equivalent piece of software to restart from Windows into Mac. This will be especially useful when using remote access.  <answer114338> BootChamp only sets the next boot, so to get from Windows to OS X strongjust reboot normally/strong!br It will boot into OS X by itself, since the default boot disk is still OS X.  <comment133990> How do you use Bootcamp to restart into Windows? <comment198899> If the default boot is Windows though ... ?