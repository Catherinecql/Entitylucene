Photos smart album for photos with no people at all <body> The macOS Photos app allows searching for photos that contain specific people. But how can one create a smart album that only contains photos that do emnot/em contain any people at all?  It is possible to create a filter that excludes a specific person.  That filter might be a solution if there only was a way to exclude emall/em persons using a wildcard or similar. 