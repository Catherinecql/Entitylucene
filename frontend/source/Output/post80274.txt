What can cause multiple processes on Mac with the same PID in some cases? <body> Why are there are many similar processes running at the same time with the same Process ID?  For instance: 2 SystemUIServer with   ul lithe same PID /li lisame user/li liboth IPv4/li /ul  <comment92632> how do you display those pid ? `ps` command ? Activity monitor ? <comment92649> It has to be the monitoring tool doing double-reporting. Two processes cannot have the same pid.