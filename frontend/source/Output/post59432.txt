How to install ImageMagick on 10.6 (Snow Leopard) using a precompiled binary (without a package manager)? <body> The wish is to manipulate images from the command line using the ImageMagick tools.   No package manager because:  ol liThe MacPorts codeport/code and Homebrew codebrew/code package managers have Xcode dependencies./li liXcode is a 4.1GB download for version 3.2.6 and I am not willing to wait for that./li liLoads of dependencies exists in the packages themselves./li liFor example ImageMagick using homebrew depends on codepkg-config/code, which fails to download: codecurl: (7) couldn't connect to host/code./li /ol  The ImageMagick.org supplied precompiled binary release is also no option as that ImageMagick-x86_64-apple-darwin12.0.0.tar.gz binary is compiled for Darwin version 12 (Mountain Lion) which results in "segmentation faults" when being run on Snow Leopard (10.6.8).  How to install ImageMagick on 10.6 (Snow Leopard) using a precompiled binary (without a package manager)?  <answer59433> ol liSearch for an old version of the binary ImageMagick release named "ImageMagick-x86_64-apple-darwin10" that runs on 10.6.8, for example: ul lihttp://tools.robodoc.net/tools/imagemagick/ImageMagick-x86_64-apple-darwin10.6.0.tar.gz/li /ul/li liDownload and extract the files./li liMove the ImageMagick-6.6.7 folder to your home folder./li liStart terminal and execute this command: code$ export MAGICK_HOME="$HOME/ImageMagick-6.6.7";export PATH="$MAGICK_HOME/bin:$PATH";export DYLD_LIBRARY_PATH="$MAGICK_HOME/lib/"/code/li liReady/li /ol  <comment134611> Thanks for that link to the old version of ImageMagick! These should be available from ImageMagick themselves... <comment186146> This answer works fine for 10.6