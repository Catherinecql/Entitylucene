(find: *.png: No such file or directory) terminal <body> I am trying to do some batch processing in terminal with texturePacker. The creator of texturePacker said that this is the way that you batch convert a directory of .pngs to .pvr.ccz.  precodefind &lt;directoryname&gt; -name \*.png | sed 's/\.png//g' | \     xargs -I % -n 1 TexturePacker %.png \         --sheet %.pvr.ccz \         --data dummy.plist \         --algorithm Basic \         --allow-free-size \         --no-trim \         --opt RGBA4444 \         --dither-fs /code/pre  But I keep getting this output in the terminal. (find: *.png: No such file or directory) Does anyone know why it would be saying that?  <answer89684> Did you specify the name of the directory containing the files: where the placeholder code&lt;directoryname&gt;/code is? Is there at least one file ending in .png in that directory?  Try with the first part of the command:  precodefind &lt;directoryname&gt; -name \*.png /code/pre  to see if you get the list of files.  <answer89688> It's likely you're not inputting the directory properly. The command you enter in should be something like: codefind /Users/me/mydir/ -name \*.png […]/code, with no angled brackets, and if you have any spaces in the directory path, be sure to put it in quotes, like so: code'/Users/me/my directory/'/code.  It's also worth noting that the command will act on any PNGs not just within the directory you give it, but also any subdirectories.  An alternate (and slightly more concise) version that just acts on the emcurrent/em directory would be:  precodefor f in *.png; do TexturePacker "$f" --sheet ${f%.*}.pvr.ccz --data dummy.plist --algorithm Basic --allow-free-size --no-trim --opt RGBA4444 --dither-fs; done /code/pre  Navigate to whatever directory you want in Terminal (easiest way is to type codecd/code, drag the folder onto the Terminal window and hit enter), then run that command.  <comment105354> I did replace it with the correct directory and all of the files in that directory are named sequentially frame0.png frame1.png etc...