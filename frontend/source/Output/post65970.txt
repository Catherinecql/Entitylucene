Launch app periodically with Automator <body> How can I launch an app in Automator periodically, like in 15 minute intervals?  <answer65971> You can also use AppleScript instead (code/Applications/Utilities/AppleScript\ Editor.app//code).  The following script starts the Terminal.app every strongseven seconds:/strong  precodeon idle     tell application "System Events"         tell application "Terminal"             run         end tell         return 7     end tell end idle /code/pre  Save this script as an strongapplication/strong and let it "stay open after run handler".  <answer66029> Using launchd, you could save the property list below as code~/Library/LaunchAgents/com.stackexchange.apple.65970.plist/code, and then load it with codelaunchctl load ~/Library/LaunchAgents/com.stackexchange.apple.65970.plist/code or by logging out and back in.  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC -//Apple Computer//DTD PLIST 1.0//EN http://www.apple.com/DTDs/PropertyList-1.0.dtd&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;     &lt;key&gt;Label&lt;/key&gt;     &lt;string&gt;com.stackexchange.apple.65970&lt;/string&gt;     &lt;key&gt;ProgramArguments&lt;/key&gt;     &lt;array&gt;         &lt;string&gt;open&lt;/string&gt;         &lt;string&gt;-jga&lt;/string&gt;         &lt;string&gt;Mail&lt;/string&gt;     &lt;/array&gt;     &lt;key&gt;RunAtLoad&lt;/key&gt;     &lt;true/&gt; &lt;!-- run before the first 15 minutes have passed --&gt;     &lt;key&gt;StartInterval&lt;/key&gt;     &lt;integer&gt;900&lt;/integer&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  codeopen -g/code opens an application on the background, but it will still open a visible window if the application wasn't running before. code-j/code also hides the application if it wasn't running. It was added in 10.8 and is only shown in the help message.  You could also add a line like this to a crontab:  precode*/15 * * * * open -jga Mail /code/pre  <comment77621> Thank you, this helps! What happens if the app is already running and doing some work? Will it be interrupted? <comment77629> I tried your solution and it works, sort of... In the mac application I try to run I put an [NSApp terminate:self] method so that it quits itself after its job is done. When I run the script it successfully launches the applications and it will do its job and quit. When the script fires again it will throw a "connection is invalid" error, which as I read means that the script tried to communicate with an application that stopped running while the script was being executed. How can I avoid this? <comment77647> @ZoltánMatók I'm sorry, but I can't exactly follow the problem your describing. Why would the Apple Script fire this connection error? I've been using variations of this apple script for different applications without problems. So I'd expect the problem to lie within your described mac application. Maybe you should open a separate question on Stackoverflow? <comment77666> Thanks, I did! Here's the link if you are interested http://apple.stackexchange.com/questions/66007/applescript-connection-is-invalid <comment77669> @ZoltánMatók Yeah, I found it already ;) I upvoted it as soon as I saw it. <comment77670> Great, thank you! <comment272696> Cool, this looks great. How would you get the app to quit after 5 minutes, then restart again?  I'm asking about Launching Google Drive Sync Client for Mac. It uses a lot of memory. So I want to sync up, quit it, and then wait 30 minutes to re-do.