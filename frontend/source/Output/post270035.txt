AppleScript Error: Can’t make {"../Resources/warning.icns"} into type number or string. (-1700) <body> Apple Script gave me that error when I changed the code of my bash script I used defaults read and write to save data in a unique file. This is my code:  precode#!/bin/bash dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)"  if [ "$(defaults read $dir/../DB started)" = "true" ]; then osascript -e 'display dialog "Non puoi avviarmi due volte!" with title "SiriSays" default button "OK" buttons {"OK"} with icon {"../Resources/warning.icns"}' exit fi defaults write $dir/../DB started true  CURRUSER="$(defaults read $dir/../DB name || id -F)"  btnpress="$(osascript -e 'display dialog "Ciao, '"$CURRUSER"'!" with title "SiriSays" default button "Ciao, Siri!" buttons {"Esci", "Cambia Nome", "Ciao, Siri!"} with icon {"../Resources/default.icns"}' -e 'button returned of result')"  if [ "$btnpress" == "Cambia Nome" ] then ../DBWriter --name cd "$(dirname "${BASH_SOURCE[0]}")" elif [ "$btnpress" == "Esci" ]; then defaults write $dir/../DB started false exit else say "Ciao, $CURRUSER!" osascript &lt;&lt;EOT set exitV to false repeat while exitV = false set results to display dialog "Scrivi una frase da farmi dire:" with title "SiriSays" default answer "" default button "OK" buttons {"Esci", "OK"} with icon {"../Resources/default.icns"} set myBtn to the button returned of results set myReply to the text returned of results if (myBtn = "OK") then if not (myReply = {}) then say myReply end if else do shell script "defaults write $dir/../DB started false" set exitV to true end if end repeat EOT exit fi exit /code/pre  Thanks. (Sorry for my bad English I'm Italian) 