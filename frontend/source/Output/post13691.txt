prevent launchctl from starting a anonymous process <body> I have installed dnsmasq and now I want to be able to start it with launchctl. The problem is that every time I start the app launchctl starts an anonyous process instead of using the specified label.  precode10247   -   0x100107c30.anonymous.dnsmasq -       0   com.example.dnsmasq /code/pre  But I want it to be  precode10247    0  com.example.dnsmasq /code/pre  The problem with the anonymous process is that I can't stop it with launchctl. When I run  precodelaunchctl stop com.example.dnsmasq /code/pre  the anonymous process still stays alive.  Here is my property file:  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple Computer//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;     &lt;key&gt;Label&lt;/key&gt;     &lt;string&gt;com.example.dnsmasq&lt;/string&gt;         &lt;key&gt;ProgramArguments&lt;/key&gt;     &lt;array&gt;       &lt;string&gt;/usr/local/sbin/dnsmasq&lt;/string&gt;         &lt;string&gt;-p&lt;/string&gt;         &lt;string&gt;2155&lt;/string&gt;         &lt;string&gt;-R&lt;/string&gt;         &lt;string&gt;-A&lt;/string&gt;         &lt;string&gt;/.dev/127.0.0.1&lt;/string&gt;         &lt;string&gt;-x&lt;/string&gt;         &lt;string&gt;/usr/local/var/run/dnsmasq.pid&lt;/string&gt;     &lt;/array&gt;   &lt;key&gt;RunAtLoad&lt;/key&gt;   &lt;false/&gt;   &lt;key&gt;KeepAlive&lt;/key&gt;   &lt;false/&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  Any idea on how to fix this?  <answer13693> That isn't actually codelaunchctl/code creating a new process; it's most likely codednsmasq/code  itself.  If it codefork()/codes into the background, codelaunchd/code can't tell; then when it talks to codelaunchd/code, codelaunchd/code creates an anonymous entry for it.  Make sure codednsmasq/code doesn't background itself.  (The code-/code listed for the regular codelaunchctl list/code output indicates that the original codednsmasq/code process exited.)  <comment14550> This very question has been asked on Stack Overflow: [link](http://stackoverflow.com/questions/5924519/mac-os-x-prevent-launchctl-from-starting-a-anonymous-process). Please [avoid cross-posting](http://meta.stackexchange.com/questions/64068/is-it-possible-to-post-questions-across-a-couple-of-the-sites-because-its-debata/64073#64073). <comment14552> sorry, was just about to delete it on SO. thaught this site would be a better place for this kind of quenstions. <comment14562> Specifically, adding "-k" (or "--keep-in-foreground") to the ProgramArguments array should do it.  After doing this, you might also want to switch KeepAlive to true (so launchd will restart it if it quits for some reason).