App that scans local network for shared folders? <body> I'm looking for an application that can scan my local network and report any shared folders it finds.  I've tried iStumbler, but it only gives you information on machines that are sharing with Bonjour.  Anything out there that looks harder, ie. shows samba, nfs, etc?  h2on edit/h2  The real issue here is that I have an external disk drive connected to my router, and I would like to make sure that the samba share I've created on it is working.  Basically I want a port scanner that is user-friendly and will give lots of information about what is really being exposed on the network.  <answer521> Finder emby default/em will show shared folders for most services.  For an more in depth look at what a machine is running you can always port scan the heck out of it. Then connecting is as easy as memorizing proper instructions over codetelnet/code or plugging into Finders 'Connect to Server' dialog.  codenmap/code (really codesudo nmap/code) is one of the fastest, most informative port scanning tools out there. It spanks the pants off any GUI for speed and service comprehension. Try something like the the following.  precode# [target ip] = Ip of the host to scan sudo nmap -PN -n -sV --version-all [target ip] /code/pre  <answer4385> Just to add more information, OS X has a portscan application since Panther (as far as I can remember). Network Utility (in /Applications/Utilities/Network Utility.app) will do a Port Scan.  I lacks the power of nmap, but it’s a quick way to see if a service is running. namp is god anyway.  img src="https://i.stack.imgur.com/NHQY0.png" alt="alt text"  <comment484> Which version of OS X are you running? Leopard and Snow Leopard show a "SHARED" section in the Finder sidebar that lists everything Mac OS could find. <comment492> that's actually what I was looking for, a port scanner.  Updating the question. <comment493> Snow Leopard 10.6.4. <comment495> btw, I'll check that one out :)