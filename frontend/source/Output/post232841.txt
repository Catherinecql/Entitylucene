Why does control+tab in Safari select the address bar? <body> When I hit control+tab (which is bound to "Show Next Tab") once, Safari selects the address bar. When I press it a second time, Safari actually goes to the next tab. Why is this and how do I fix it?  After pressing control+tab once:  img src="https://i.stack.imgur.com/VLqtc.png" alt="one ctrl-tab"  After pressing control+tab again:  img src="https://i.stack.imgur.com/9ifzZ.png" alt="two ctrl-tabs" 