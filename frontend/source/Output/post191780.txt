How do I configure my Terminal app with bash and iTerm2 with zshell and oh my zsh? <body> The Terminal app uses bash by default. And I don't want to change it. However I want to configure iTerm2 with zshell and oh my zsh. and I don't want bash on it. The instructions I checked online, make zshell default in both apps.  <answer191804> If your default system shell is codebash/code, your Terminal should start with it. You can check it on General tab under Terminal Preferences. Should look like the following:  img src="https://i.stack.imgur.com/ae6AH.png" alt="enter image description here"  If bash isn't your default shell, you can change it by typing:  precodechsh -s /bin/bash /code/pre  To configure iTerm2 with codezsh/code you have to open Preferences and change the command on General tab on your default profile. You have to enter code/bin/zsh/code or whatever shell you want.  img src="https://i.stack.imgur.com/hOzPm.png" alt="some text here"  To install oh-my-zsh type the following:  precodesh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)" /code/pre  <comment283977> Should it actually be `chsh -s /bin/bash` (-s, not -c)? I get `chsh: illegal option -- c`...