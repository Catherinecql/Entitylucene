Can wildcards be used in the website whitelist/blacklist in macOS Parental Controls? <body> In the Web section of Parental Controls in System Preferences in macOS, under Customize, can wildcards be used under "Always allow these websites" and "Never allow these websites"? For example, if I want to allow/deny codeexample.com/code and all subdomains thereof, will it work if I add an entry for code*.example.com/code? Or is this implicit by just adding codeexample.com/code?  <comment308309> Have you tried it? I would imagine `example.com` also blocks all subdomains, but maybe not...