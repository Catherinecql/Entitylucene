Command Line - Enable Remote Login and Remote Management <body> For my job, before I deploy a new Mac laptop, I need to enable these two settings:  ol liSystem Preferences - Sharing - Remote Login.  Once enabled I also need to add the admin account that previously I have created by selecting the little + symbol under Only these users:/li liSystem Preferences - Sharing - Remote Management - (the little pop up windows comes out) and I select all the features/li /ol  How do I achieve the above two process via command line?  <answer278763> Modifying remote login and remote management settings is done with two different commands - both executed as root:   ul lisystemsetup /li likickstart/li /ul  To enable remote login for members of the admin group enter:  precodesudo systemsetup -setremotelogin on /code/pre  To restrict access use codedseditgroup/code. First check if it exists and add it if necessary:  precodedseditgroup com.apple.access_ssh dseditgroup -o create -q com.apple.access_ssh /code/pre  and add a user group:  precodesudo dseditgroup -o edit -a admin -t group com.apple.access_ssh /code/pre  To enable remote management for admin users enter:  precodesudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -users admin -privs -all -restart -agent -menu /code/pre  To check other options enter:  precodesudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart --help /code/pre  <comment350832> Which deployment solution do you use? DeployStudio, Casper or scripts? <comment350842> neither.  i just ssh into the machine to do stuff.  But I need to enable the two steps above before I do that for different reasons.