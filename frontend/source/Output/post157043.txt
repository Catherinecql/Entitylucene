Enabling Apache <body> I cannot find the web sharing option in the system preferences under "Sharing"(where the web sharing is supposed to be). I am using Mac Os x 10.9.4  <answer157124> emWarning: The normal user will be happy with Server.app/em  Apache is still available, but the user interface is gone. If you are comfortable configuring Apache the traditional way via config files, you can activate it by running codesudo apachectl start/code from the shell. You find the config files in code/etc/apache2/code. Be prepared to restore your configuration on OS updates.  <answer157171>   Just run codesudo apachectl start/code.  codeapachectl/code is a shell script and codesudo apachectl start/code runs codelaunchctl load -w /System/Library/LaunchDaemons/org.apache.httpd.plist/code as root, which modifies code/var/db/launchd.db/com.apple.launchd/overrides.plist/code to enable apache permanently.  To make codelocalhost/~yourusername/code point to code~/Sites//code, run  pre class="lang-none prettyprint-override"codeecho '&lt;Directory "/Users/'$USER'/Sites/"&gt;   Options Indexes Multiviews   AllowOverride AuthConfig Limit   Order allow,deny   Allow from all &lt;/Directory&gt;'|sudo tee -a /etc/apache2/users/$USER.conf /code/pre  and run codesudo apachectl restart/code.  To enable PHP, uncomment  pre class="lang-none prettyprint-override"code#LoadModule php5_module libexec/apache2/libphp5.so /code/pre  in code/etc/apache2/httpd.conf/code and run codesudo apachectl restart/code.  <answer157190> If you're not comfortable with the command line methods,  you might consider installing MAMP, which offers a convenient GUI for starting, stopping and basic configuration: http://www.mamp.info/en/ 