Can I run a torrent client + Plex Media Server as "services"? <body> I'm using an old iMac as a torrent client &amp; Plex Media Server.  But I would like those to run as "services", i.e., not as part of a User account, and without having to log in.  <answer116827> You can use stronglaunchd/strong, tmux and this script to launch rTorrent as a daemon.  This post describes how to install rTorrent and run it as a daemon using tmux. First, install software:  precode$brew install rtorrent --with-xmlrpc-c $brew install tmux /code/pre  Parameter –with-xmlrpc-c is needed to use GUI clients with rTorrent. rTorrent needs terminal to run. We provide virtual terminal using tmux. To autostart rTorrent create launchd sript and place it to  precode/Library/LaunchAgents/rtorrent.plist /code/pre  The script:  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;     &lt;key&gt;Label&lt;/key&gt;     &lt;string&gt;rtorrent&lt;/string&gt;     &lt;key&gt;ProgramArguments&lt;/key&gt;     &lt;array&gt;         &lt;string&gt;/usr/local/bin/tmux&lt;/string&gt;         &lt;string&gt;new-session&lt;/string&gt;         &lt;string&gt;-s&lt;/string&gt;         &lt;string&gt;rtorrent&lt;/string&gt;         &lt;string&gt;-d&lt;/string&gt;         &lt;string&gt;rtorrent&lt;/string&gt;     &lt;/array&gt;     &lt;key&gt;KeepAlive&lt;/key&gt;     &lt;true/&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  Load script:  precode$sudo launchctl load -w /Library/LaunchAgents/rtorrent.plist $launchctl list | grep rtorrent /code/pre  If all is ok you will see something like this:  precode- 1 rtorrent 4868 - 0x7f872252ff90.anonymous.rtorrent /code/pre  Now you can attach to tmux session and start using rTorrent:  precode$tmux attach-session -t rtorrent /code/pre  To restart script unload it and load again:  precode$sudo launchctl unload -w /Library/LaunchAgents/rtorrent.plist $sudo launchctl load -w /Library/LaunchAgents/rtorrent.plist /code/pre  To view launchd logs run:  precode$sudo launchctl log level debug $tail -f /var/log/system.log | grep launchd /code/pre  The rTorrent installation is over, now you can install GUI client. There is free client for Mac OS X called Nativa. It not very good, but you can try it. To use any client you need to add this in your .rtorrent.rc:  precodescgi_port = 127.0.0.1:5000 /code/pre  Source  <comment135948> Transmission works according to this thread: http://apple.stackexchange.com/questions/30310/torrent-client-for-os-x-that-can-be-shared-between-multiple-user-accounts <comment137537> I have added the step from your source to the answer posted. In the future, please don't simply link offsite. We like answers to be complete and robust, not just links to other places on the net. This isn't Google. Many thanks for contributing. <comment137540> Services are tasks that can be called from  the serve menu in applications. Things that ruin in the background like you say are daemons.