Equivalents for genisoimage and qemu-img on Ubuntu <body> How do I achieve the same result on OS X 10.9 as I do on Ubuntu with commands:  precode$ genisoimage -output init.iso -volid cidata -joliet -rock user-data meta-data /code/pre  and  precode$ qemu-img convert -O raw disk.img disk.img.raw /code/pre  <answer171997> precode$ hdiutil makehybrid -o init.iso -hfs -joliet -iso -default-volume-name cidata config/ /code/pre  Where config/ contains meta-data and user-data.  precode$ brew install gemu /code/pre  <answer229457> You can use codemkisofs/code instead of codegenisoimage/code - they are almost identical (mkisofs is actually newer and better maintained, while genisoimage is debian's old fork of it).  It comes with cdrtools, which you can install from brew:  precodebrew install cdrtools /code/pre 