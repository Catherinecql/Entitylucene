Launchd Multicast Socket Listener <body> I'm not sure if I've just missed some documentation or not, but I'm having a hard time figuring out how to set up a codelaunchd/code agent that can be triggered whenever a datagram is received within a multicast group.  What I'd like to know is what a codelaunchd/code plist should look like to provide a simple datagram listener that is bound to a multicast group.  Here's what I've been trying:  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;     &lt;key&gt;Label&lt;/key&gt;     &lt;string&gt;foo.bar&lt;/string&gt;     &lt;key&gt;Program&lt;/key&gt;     &lt;string&gt;/foo/bar&lt;/string&gt;     &lt;key&gt;Sockets&lt;/key&gt;     &lt;dict&gt;         &lt;key&gt;Listeners&lt;/key&gt;         &lt;dict&gt;             &lt;key&gt;SockType&lt;/key&gt;             &lt;string&gt;dgram&lt;/string&gt;             &lt;key&gt;SockServiceName&lt;/key&gt;             &lt;string&gt;12345&lt;/string&gt;             &lt;key&gt;SockFamily&lt;/key&gt;             &lt;string&gt;IPv4&lt;/string&gt;             &lt;key&gt;MulticastSocket&lt;/key&gt;             &lt;string&gt;224.0.0.253&lt;/string&gt;         &lt;/dict&gt;     &lt;/dict&gt;     &lt;key&gt;inetdCompatibility&lt;/key&gt;     &lt;dict&gt;         &lt;key&gt;Wait&lt;/key&gt;         &lt;false/&gt;     &lt;/dict&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  But it just generates codegetaddrinfo/code errors ("nodename nor servname provided, or not known"). Removing the multicast group to just setup a UDP port seems to work fine, but I don't really want to have to hard code an IP address or local DNS.  Are there any good examples of how to create a multicast listening launch agent or daemon using codelaunchd/code? Or am I missing some other step, or misunderstanding what data I should be entering for a multicast group?  <answer148745> I set this one up using LaunchControl.    precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;     &lt;key&gt;Label&lt;/key&gt;     &lt;string&gt;foo/bar&lt;/string&gt;     &lt;key&gt;Program&lt;/key&gt;     &lt;string&gt;/foo/bar&lt;/string&gt;     &lt;key&gt;Sockets&lt;/key&gt;     &lt;dict&gt;         &lt;key&gt;Sock1&lt;/key&gt;         &lt;dict&gt;             &lt;key&gt;MulticastGroup&lt;/key&gt;             &lt;string&gt;224.0.0.253&lt;/string&gt;             &lt;key&gt;SockFamily&lt;/key&gt;             &lt;string&gt;IPv4&lt;/string&gt;             &lt;key&gt;SockProtocol&lt;/key&gt;             &lt;string&gt;UDP&lt;/string&gt;             &lt;key&gt;SockServiceName&lt;/key&gt;             &lt;string&gt;12345&lt;/string&gt;             &lt;key&gt;SockType&lt;/key&gt;             &lt;string&gt;dgram&lt;/string&gt;         &lt;/dict&gt;     &lt;/dict&gt;     &lt;key&gt;inetdCompatibility&lt;/key&gt;     &lt;dict&gt;         &lt;key&gt;Wait&lt;/key&gt;         &lt;true/&gt;     &lt;/dict&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  <comment161339> What is a multicast listening launch agent? And a datagram listener? Although I have created numerous launch agents, I have never seen these terms/jargon. <comment161506> You can create launch agents with a list of sockets, if they receive a message on one of these then the program is launchd with the message on standard input. Multicasting is UDP (datagrams) where a message is simply sent out to all machines in the network with no need to connect to them first, and any that choose to can respond. <comment175447> Thanks for the response! Were you able to verify this in any way? I've given the same basic file a try, simply using `cat` as the program and redirecting `stdout` and `stderr` (using the appropriate plist keys) but it's not producing any output. Instead I'm getting a bunch of "no such file or directory" errors from `execv()`. I've tried replacing `cat` with a simple shell script and confirmed the correct path, but that won't work either. <comment177139> how may i test a working launchd Multicast Socket Listener daemon means how may i invoke such a datagram? <comment177335> You can test sending of a UDP message by doing something like `echo 'My message' | nc -uw 1 localhost 6000` to send the message to a service listening to port 6000 on your local machine. Not sure if `nc` supports sending multicast messages (I don't see an option) but it lets you test when sending directly to a single machine.