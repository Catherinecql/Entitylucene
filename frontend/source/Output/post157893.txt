What is the equivalent of netstat -tln on OS X? <body> What is the equivalent of Linux's codenetstat -tln/code on OS X?  Options:  precode-l, --listening (Show only listening sockets. (These are omitted by default.) --numeric , -n (Show numerical addresses instead of trying to determine symbolic host, port or user names.) -t, --tcp /code/pre  <answer158029> Should be this command:  precodesudo lsof -iTCP:$PORT -sTCP:LISTEN  /code/pre  Who is listening on a given TCP port on Mac OS X?     <comment185671> What does `netstat -tln` actually do? <comment185672> You can just open a Terminar an execute on it. It will work. <comment185675> @jherran No, OS X is using a BSD-based netstat which accepts different sets of options. <comment185676> @patrix in Linux, it lists all open TCP ports on the machine, and whether it listens locally (127.0.0.1) or to remote connections (0.0.0.0).