/usr is not writable. Run this installer as root error when installing Phoronix <body> I'm trying to install Phoronix Test Suite. Once I codecd/code to the folder it downloads, I try codesudo ./install-sh/code which gives the error codeERROR: /usr is not writable. Run this installer as root or specify a different directory prefix as the first argument sent to this script./code  I thought that codesudo/code would run the installer as root, so now I am stuck as to what to do next?  <comment298561> Are you running OS X 10.11.x El Capitan?  If yes, you need to disable SIP.  Have a look at [How to Disable System Integrity Protection (rootless) in OS X El Capitan](http://osxdaily.com/2015/10/05/disable-rootless-system-integrity-protection-mac-os-x/), and also read [About System Integrity Protection on your Mac](https://support.apple.com/en-us/HT204899). <comment298562> Phoenix does not follow standard Unix installation conventions i.e. install in /usr/local/bin or /opt but tries to install as part of the OS in /usr/bin so does not work under SIP. I would also be worried by  For tests that rely upon a compiler, Apple's XCode with GCC and LLVM can be utilised. as Xcode does not come with gcc now