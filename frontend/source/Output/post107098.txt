OS X's hidden preferences disappeared after Mavericks upgrade? <body> In past OS X versions, I used some commands to set preferences on my Mac.  For example, I used the following command to enable selection of text in Quick Look:  precodedefaults write com.apple.finder QLEnableTextSelection -bool TRUE; /code/pre  …and the following command to create a stack on the Dock of recently-used apps:  precodedefaults write com.apple.dock persistent-others -array-add '{ "tile-data" = { "list-type" = 1; }; "tile-type" = "recents-tile"; }' /code/pre  In my Mac that I just upgraded they are all working as before, but in other Mac, which I initialised before upgrading they don't work. Does somebody knows a way to make them work again or an alternative for them?  <answer107256> Both of those still work for me, but you do sometimes need to run defaults commands again after upgrading OS X. If you run the commands again, quit and reopen applications to apply the changes:  precodekillall Dock Finder /code/pre  (If you also want to enable selecting text in Quick Look windows shown from other applications than Finder, you can use codedefaults write -g QLEnableTextSelection -bool true/code instead of codedefaults write com.apple.finder QLEnableTextSelection -bool true/code.)  <answer159237> To create a stack on the Dock of recently-used apps I solved using Thinkertool (http://www.bresink.com/osx/TinkerTool.html) which allow to enable lot of hidden preferences including the stack of recently-used apps.  <comment125616> What happens if you rerun those defaults commands after the OS upgrade <comment125834> Your version for QLEnableTextSelection has worked but I still  can't generate the stack for recent App or disable NSGlobalDomain NSDocumentSaveNewDocumentsToCloud ( defaults write NSGlobalDomain NSDocumentSaveNewDocumentsToCloud -bool false )... <comment125835> I tried pasting working .plist in initialized Mac but it still don't work... <comment125876> @Matte.Car Did you quit and reopen applications to apply the changes? `defaults write NSGlobalDomain  NSDocumentSaveNewDocumentsToCloud -bool false` still works for me. <comment125879> no, wrong, NSGlobalDomain NSDocumentSaveNewDocumentsToCloud works for me too. The only thing that I can't do is to create a stack of recent launched apps...