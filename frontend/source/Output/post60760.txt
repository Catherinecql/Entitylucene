What is the PATH environment variable and how do I set it up to use the Android SDK? <body> I'm a mac noob, and a terminal noob.  I'm used to installing things in Windows, and now I'm trying to set up Eclipse with the Android SDK on Mac and I'm having some trouble.  I downloaded Eclipse and stuck it in a folder I called Android, then downloaded the Android SDK and stuck it in that folder as well. The next step of these instructions says this:     Update your PATH environment variable to know about the Android tools.   There are different ways of setting your default PATH variable. I use ~/.bash_login but ~/.bash_profile also works. The Android tools are in the tools directory of the Android SDK. So this is what I added to .bash_login:   export PATH=$PATH:/Android/android-sdk-mac_x86/tools   I have absolutely no idea what any of that means. Can someone help to dumb it down a little for me? That instruction presupposes emway/em too much Mac/unix knowledge on my part.  <answer60773> The codePATH/code environment variable is simply an order list of directories over which (typically) executables are searched for and run. In short, to get you through the setup you'll need to edit code/Users/&lt;username/.bash_profile/code to include  precodeexport PATH=$PATH:/Android/android-sdk-mac_x86/tools /code/pre  This is the most painless way to do that. strongNote, this assumes that you have the same SDK install path. Modify the text to suit otherwise./strong  ol liOpen up a Terminal window (don't cringe).  Use Spotlight ( kbd⌘/kbdkbdSPC/kbd ) and start typing "terminal".  Hit return when you see it highlighted. /li liCopy and paste the following into the terminal window that opens up and hit enter. /li /ol  codeecho 'export PATH=$PATH:/Android/android-sdk-mac_x86/tools' &gt;&gt; ~/.bash_profile/code  That's it. If you already have a code.bash_profile/code, this will add that line to the end of the file.  If you don't already have one, it will create one for you.  <comment131782> My answer in StackOverFlow to an upgrade question where path gets changed might help as well. [Android/iOS issue fix in MACOSX](http://stackoverflow.com/questions/20080325/cordova-and-setting-android-using-command-line/20286967#20286967) <comment148010> However this does not work for GUI apps