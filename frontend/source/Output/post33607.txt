10.7 Launchd + rvm <body> This question fits somewhere between server fault, stackoverflow and askdifferent.   I am installing some web apps on my Lion server using webappctl, which hooks into apaches vhost configs to setup up a reverse proxy (simply mod_proxy). It uses launchd to start the server, in my case unicorn serving a Ruby on Rails app, listening on TCP port 5000. I use RVM (mixed-mode) to facilitate in separate ruby versions and gemsets for each webapp.  The problem lies with RVM and launchd, and the complete environment not being available for launchd.  I tried to start from launchd using code/bin/bash -l -c 'unicorn_rails -l 5000'/code but that gave me bash usage errors in the logs.   Any suggestions for making RVM play nice with launchd?  <answer100166> I found a way to make a codeRVM/code-based coderake/code command work with codelaunchd/code:  precode/bin/bash -l -c '/absolute/path/to/myscript.sh' /code/pre  My script was calling coderake/code. I wasn't calling it directly.  Have you tried using an absolute path for your codeunicorn_rails/code?  The code.plist/code looks like:  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;         &lt;key&gt;Label&lt;/key&gt;         &lt;string&gt;mylabel&lt;/string&gt;         &lt;key&gt;ProgramArguments&lt;/key&gt;         &lt;array&gt;                 &lt;string&gt;/bin/bash&lt;/string&gt;                 &lt;string&gt;-l&lt;/string&gt;                 &lt;string&gt;-c&lt;/string&gt;                 &lt;string&gt;/absolute/path/to/myscript.sh&lt;/string&gt;         &lt;/array&gt;         &lt;key&gt;StartCalendarInterval&lt;/key&gt;         &lt;dict&gt;                 &lt;key&gt;Hour&lt;/key&gt;                 &lt;integer&gt;10&lt;/integer&gt;                 &lt;key&gt;Minute&lt;/key&gt;                 &lt;integer&gt;00&lt;/integer&gt;         &lt;/dict&gt;     &lt;/dict&gt;  &lt;/plist&gt; /code/pre  <comment38315> This question would get a better response on Superuser <comment43351> A lot of Apple related questions are being migrated to **Ask Different** that really shouldn't be. The level of technical knowledge on Ask Different is low compared to **StackOverflow**, **ServerFault**, and **SuperUser**. Just because it's Apple hardware/software doesn't mean it doesn't belong on those sites! So stop migrating just because some Win or open source troll sees the word `Mac` <comment131837> What were the bash errors?