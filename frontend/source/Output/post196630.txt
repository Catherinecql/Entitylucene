environment.plist adds extra characters? <body> OSX Yosemite 10.10.3  I used environment.plist  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;   &lt;key&gt;Label&lt;/key&gt;   &lt;string&gt;my.startup&lt;/string&gt;   &lt;key&gt;ProgramArguments&lt;/key&gt;   &lt;array&gt;     &lt;string&gt;sh&lt;/string&gt;     &lt;string&gt;-c&lt;/string&gt;     &lt;string&gt;     launchctl setenv CX_LICENSE LS     launchctl setenv CX_LIC_SERVER 127.0.0.1     &lt;/string&gt;    &lt;/array&gt;   &lt;key&gt;RunAtLoad&lt;/key&gt;   &lt;true/&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  When I go to terminal and type set I get this  precodeCX_LICENSE=$'LS\r' CX_LIC_SERVER=$'127.0.0.1\r' /code/pre  then when I try to launch a program that needs to have CX_LICENSE set up it fails.   If I in the open new terminal and run codeCX_LICENSE=LS/code and then start the same program from the command line using codeopen -n application.name/code it runs correctly.  ul liIf I do codeecho -$CX_LICENSE-/code in the first terminal I get code-LS/code/li liIf I do codeecho -$CX_LICENSE-/code in the second terminal I get code-LS-/code/li /ul  I find this very strange. Could someone explain and probably suggest a fix?  <comment234930> Do you mean ~/.MacOSX/environment.plist If so this only worked up to Mountain Lion <comment234935> You have carriage returns in your plist. More than likely the plist contains windows line endings. You need to change the line endings to newline only. You can attempt to fix the plist with `tr -d '\r'  plist  newplist` or find a text editor that uses "unix" line endings and rewrite your plist. <comment235086> Do you want to create an answer from your comment? It did help. Thank you