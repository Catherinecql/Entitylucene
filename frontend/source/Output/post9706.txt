Where is the Xcode installer located? <body> When I browse the Mac App Store, the buy button of the Xcode app is shown as "installed" so that I can't buy it. But I can't find it in my Applications folder either.  How can I locate the app? Or how can I change the "installed" button to "buy" button?  <answer9707> You could try inspecting the output of  precode$ pkgutil --pkgs /code/pre  to see if any of the names look like the package that claims to be installed, then  precode$ pkgutil --pkg-info thatpackage /code/pre  to see where it was installed.  If it's not actually found there, you could try  precode$ sudo pkgutil --forget thatpackage /code/pre  and see if the App Store will let you re-download; strongbut/strong at that point I'd probably contact App Store support first, as mucking with the package database might cause bigger problems (like, conceivably, forgetting that you ever bought the program — although I would hope Apple's programmers were smarter than that).  <answer9708> Did you already have a non-App Store version of the app installed?  When I installed Opera from the App Store, it replaced my existing version of Opera, which I had not installed in codeApplications/code.  You can locate the app by using Spotlight.  Click the magnifying glass in the top-right corner of your screen and type the name of the application in.  Click em"Show all"/em, right-click on the application, and select em"Open enclosing folder"/em.  <answer9981> FYI, I had a similar problem but eventually noticed (via spotlight search for 'xcode') that rather than installing the actual app, it had downloaded an app in /Applications called, "Install XCode" which was 4.58 GB.  Running that does the actual install of XCode 4.  <comment10149> Thank you! I followed your instructions and solved the problem. <comment10150> The app file is located in a system folder so that it could not be found by Spotlight. But thanks anyway =) <comment10381> By the tags, I guess the application in question is XCode (4?). It should be installed under /Developer, but I still have 3.2.5 there, personally, despite having bought version 4 from the App Store.