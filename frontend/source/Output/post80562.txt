Change main keyboard layout in OS X <body> I have two active keyboard layouts - US english and russian. I have russian as my primary system language and the option "allow different input language for different documents" is active. But when I open any app keyboard automatically switches to russian, while I want it to be english by default. Is there any way to change the default keyboard layout without changing system language?  <answer80567> When I tried enabling the setting (Input source options: Allow a different one for each document), new windows seemed to just use the current input source.  Anyway, you could try running codesudo /System/Library/CoreServices/Setup\ Assistant.app/Contents/MacOS/Setup\ Assistant/code and selecting US as the default input method. (You have to go through the steps of creating a new account, but you can delete it later.) It modifies at least code/var/root/Library/Preferences/ByHost/com.apple.HIToolbox.*.plist/code.  If that doesn't work, try running codesudo languagesetup/code and selecting English, or codesudo defaults write /Library/Preferences/.GlobalPreferences.plist AppleLocale en_US/code and restarting.  <answer80585> img src="https://i.stack.imgur.com/aYUMW.png" alt="enter image description here"Try the option strongUse the same one in all documents/strong and with a keyboard shortcut you can easily switch between different input methods, for example, codeCmd + Space/code.  <comment92993> It's kind of difficult to understand what you propose to do here, especially the part between the "" <comment140676> What system are you on? On my Mac running Mavericks newly started apps just keep the keyboard layout that was selected before.