Turn off the SSH Agent <body> How do I turn the SSH Agent off? I'm having troubles with Vagrant and it indicates that I should disable my SSH Agent before continuing, yet I can't find a straight forward answer. How would I do it?  Temporary answers work too. I'm happy with how my environment is setup, it's just this particular situation where I need to verify that disabling the SSH Agent corrects the issue, even if I then turn it back on.  <answer247958> unset the environment variable code$SSH_AUTH_SOCK/code. In codebash/code, from where you run the codevagrant/code commands, simply:  precodeexport SSH_AUTH_SOCK="" /code/pre 