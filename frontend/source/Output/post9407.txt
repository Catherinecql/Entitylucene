How to force a diff format for TextMate? <body> I have this alias:  precodealias hgd='hg diff|mate' /code/pre  The problem is that for some reason the temporary file thus created will be a code.txt/code, while I'd need it to have the codediff/code syntax highlight.  If I manually change the highlight to codediff/code, then every code.txt/code I'll open in future will use the codediff/code highlight too.  What can I do to fix that?  <answer9410> I don´t have any mercurial project myself, but if you do this in svn (eg. with codesvn diff -r 100:099/code) it generates a .txt too, but it uses the diff highlighting - maybe there is an update to the diff bundle available in your installation?   If not, you can still re-write your alias to something like codehg diff &gt; /tmp/hg.diff &amp;&amp; mate /tmp/hg.diff/code  <answer9411> Doing this with an alias won't work, but you can write a bash function that will handle this. Something like:  precodefunction hgd {     T=`mktemp /tmp/hgdiff.XXXXXX.diff`     hg diff &gt; $T     mate -w $T     rm $T } /code/pre  in your code.bash_profile/code will work. This acts closer to what you're trying to accomplish in that it leaves no trace of the temporary file on disk when you're done looking at it with TextMate.  <answer9504> TextMate can use either the file extension emor/em the first line of a file to divine what filetype it is.  For my diff bundle (I think it's the original), this is:  precodefirstLineMatch = '(?x)^         (===\ modified\ file         |==== \s* // .+ \s - \s .+ \s+ ====         |Index:\          |---\ [^%]         |\*\*\*.*\d{4}\s*$         |\d+(,\d+)* (a|d|c) \d+(,\d+)* $         |diff\ --git\          )     '; /code/pre  I don't have mercurial, but it appears as though its diff's first line may look something like codediff -r 2278160e78d4 hello.c/code.  As such, you can add another line to that big OR switch:  precode        ...         |diff\ --git\          |diff\ -r\          )     '; /code/pre  Note that the final character in the line is a space.  As another alternative, it appears as though you may be able to define your alias as codealias hgd='hg diff --git|mate'/code to use the same format as git, which is already recognized by TextMate. 