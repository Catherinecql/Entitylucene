Flashing question mark, low hard drive space <body> When I booted my Mac last night I got a flashing folder against a gray background. The computer started fine this morning, but when I closed the lid and opened it up again I had to reboot, and now it's persistently in this flashing folder state.  Booting while holding down kbd⌘ Cmd/kbd-kbdR/kbd brings me to OS X Utilities. Running "Repair disk" from disk utilities indicates there's no problem, but I did notice that it also says I only have 16 MB free on a 110GB hard drive.   Might this be the problem? If so, is there some way to delete data from my computer while in this state, to clear enough space to boot correctly?  <answer173825> strongThough the following should be valid for non-encrypted volumes and unlocked encrypted volumes, it doesn't help much because the main volume here is encrypted and locked. So skip to FileVault2-encrypted volumes/strong  strongNon-encrypted volumes/strong  Almost no free space on your start volume is a problem. At least 10% free space are recommended.  It's indeed possible to remove files &amp; folder in Recovery Mode but if you have an external disk/thumb drive (hfs+ or fat32-formatted) you may move them instead:  ul liattach your external device (if you have one)./li liIf FileVault2 is enabled on your main volume start Disk Utility, right click the volume and unlock it/li liQuit Disk Utility and open /Utilities/Terminal in the menubar/li lienter codecd /Volumes/NameOfYourMainVolume/Users/accountname/code to change to your user folder (example: codecd "/Volumes/Macintosh HD/Users/ausername"/code). To get the names/mountpoints of your main volume/external disk enter codediskutil list/code or codedf/code./li lienter codels -la/code to show all files and folders    Example:  precodedrwxr-xr-x+ 56 ausername  staff   1904 20 Feb 23:45 . drwxr-xr-x+  7 root       admin    238 15 Jun  2012 .. -rw-------   1 ausername  staff      3 21 Nov 16:11 .CFUserTextEncoding -rw-r--r--@  1 ausername  staff  43012 20 Feb 23:35 .DS_Store drwxr-xr-x+  3 ausername  staff    102 15 Feb 17:41 .TemporaryItems drwxr-xr-x   4 ausername  staff    136  2 Nov 23:22 SomeFolder /code/pre/li linow move further with codecd SomeFolder/code/li lienter codels -la/code to show all files and folders    Example:  precodedrwxr-xr-x+  7 ausername  staff        238 15 Jun  2012 .. -rw-------   1 ausername  staff 4736527884 16 Okt 22:33 BigFile.mov drwxr-xr-x   4 ausername  staff        136  2 Nov 23:22 UnwantedFolder /code/pre/li lito move BigFile.mov to your external device enterbr codemv BigFile.mov /Volumes/ExternalDevice//codebr or to move UnwantedFolder to your external device enterbr codemv UnwantedFolder /Volumes/ExternalDevice//code./li liif you don't have an external drive remove folders and files with coderm -dr UnwantedFolder/code to remove UnwantedFolder and all its content or coderm BigFile.mov/code to remove BigFile.mov (4.7 GB in the example above). coderm *.mov/code removes all .mov files in your working directory.  emrm is a powerful command to delete a file or directory without confirmation by default. Because of this behavior, users should really be sure before deleting files or folders. So double check where you are  - enter pwd to print your working directory - and what you want to delete!/em/li /ul  strongFileVault2-encrypted volumes/strong  ul listarted to Recovery Mode open Disk Utility  img src="https://i.stack.imgur.com/VaXbQ.png" alt="diskutil1"  If you had one partition spanning your whole hard drive and encrypted it with FileVault2 it's completely normal that you have only some MB available space left. That's unallocated free space on your hard drive. In the example above 12,7 MB are left./li lihighlight your FileVault2 volume and unlock it.  img src="https://i.stack.imgur.com/i8wma.png" alt="diskutil2"/li lienter the File Vault password  img src="https://i.stack.imgur.com/HpIVw.png" alt="diskutil3"/li liif you succeed and the encrypted volume isn't corrupted your FileVault2 volume will be mounted. Check the available free space. In the example below the availbale free space on the volume is 52,8 GB  img src="https://i.stack.imgur.com/v9hVa.png" alt="diskutil4"   If the unlocking fails - using the correct password - the FileVault2 volume probably is corrupted. The only possible solution then is completely erase/repartition the hard drive and restore a backup./li /ul  <comment204560> How long ago is your last backup and can you afford to delete some files or applications to then reinstall them again? (For instance, you could reinstall Word or iMovie or GarageBand easily and not lose your data or settings) <comment204561> I would delete something in /Applications unless you have some large files you can download again (torrents?) or media you can rip from CD again. Also, if the OP has a backup, then anything can be removed safely... <comment204562> Thanks, that's greatly appreciated (and nice touch incorporating my username!). Though I am a Mac novice (as is probably obvious), I'm actually very comfortable with the command line. But I'm still having problems. I used Internet Recovery, and when I open the terminal as you instruct, the only folder in /Volumes is 'Mac OS X Base System', which includes 'Install OS X Mountain Lion.app' and 'usr' but not 'Users' (and the find command shows no 'Users' directory exists), so it seems like my actual hard drive volume isn't accessible from here. <comment204590> The output of `diskutil list` shows `/dev/disk0s2` is the Apple_HFS Mac OS X Base System (1.2GB), and includes the output      `0: FDisk_partition_scheme *120.0 GB disk12     1: 0xEE 120.0 GB disk12s1`  But the mount command seems to fail: running `diskutil mount -mountPoint /Volumes/myhd /dev/disk12s1` gives the error:  `Volume on disk12s1 failed to mount`  If it's relevant, my hard drive is encrypted. Am I doing something obviously wrong? <comment204602> @ausername But now i really doubt that low disk space causes the question mark: Enabling FileVault on your main volume always leaves some *unallocated space* in the MByte range on your main drive. Maybe your FileVault got corrupted. I think we have to start from scratch.