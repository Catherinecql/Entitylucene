Set environment variable for applications on startup <body> How do you set an environment variable on startup so that it can be used in applications?  For example, codeATOM_HOME/code is used by Atom.  <answer267026> h1Solution/h1  The solution uses two files: codeenvironment.plist/code and codeenvironment.conf/code.  codeenvironment.plist/code should be placed in code~/Library/LaunchAgents/code for a per user solution (recommended), or in code/Library/LaunchAgents/code for a global solution (not recommended – potential security loophole).   codeenvironment.conf/code can be placed almost anywhere. code$PATH_TO_ENVIRONMENT_CONF/code can be relative, but only to the 'default' environment variables, or any that are defined in a codeEnvironmentVariables/code key. sup1/sup  codeenvironment.plist/code:  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;   &lt;key&gt;Label&lt;/key&gt;   &lt;string&gt;local.launchd.environment&lt;/string&gt;   &lt;key&gt;ProgramArguments&lt;/key&gt;   &lt;array&gt;     &lt;string&gt;sh&lt;/string&gt;     &lt;string&gt;-c&lt;/string&gt;     &lt;string&gt;xargs -L 1 launchctl &amp;lt; $PATH_TO_ENVIRONMENT_CONF&lt;/string&gt;   &lt;/array&gt;   &lt;key&gt;RunAtLoad&lt;/key&gt;   &lt;true/&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  codeenvironment.conf/code:  precodesetenv ATOM_HOME $HOME/.config/atom setenv SOME_VAR "Use quotes if spaces are necessary" /code/pre  h1How it works/h1  When a user logs in, the LaunchAgents in code~/Library/LaunchAgents/code are executed. In this case, codesh -c xargs -L 1 launchctl &lt; $PATH_TO_ENVIRONMENT_CONF/code is executed. codelaunchctl/code manages daemons and agents. In this case, we are using it to set an environment variable accessible to all applications and the shell.   precodexargs -L 1 launchctl &amp;lt; $PATH_TO_ENVIRONMENT_CONF  xargs                     # xargs converts stdin to command line arguments xargs -L 1                # Tells xargs to invoke launchctl for each line            launchctl      # Run launchctl with the arguments                      &amp;lt; # Since a plist is an xml document, angle brackets must be escaped.                        &amp;lt; $PATH_TO_ENVIRONMENT_CONF                           # This tells xargs to read input from the file at $PATH_TO_ENVIRONMENT_CONF /code/pre  This solution can also be extended to other launchctl subcommands.  