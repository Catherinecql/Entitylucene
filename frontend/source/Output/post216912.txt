How can I tell if a particular directory is connected to a remote machine? <body> I've found various ways to check if a particular directory is mounted under NFS (here, for example), but none of them seem to work on OS X. strongHow can I check if a particular directory is an NFS directory in the shell on a Mac?/strong  <answer240342> The command codedf/code will include a list of all NFS shares, and their mount points. If a directory's path includes an NFS mount point, it stored on that machine. 