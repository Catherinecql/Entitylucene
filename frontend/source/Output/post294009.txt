Converting 'top' output values in arithmetic values <body> i need to monitor the memory of a program and for that i use the command top.  precodeMEMO1=`top -l 1 | awk '/program/ {print $8 }  -- returns memory MEMO2=`top -l 1 | awk '/program/ {print $10 }  -- returns compressed memory /code/pre  I use it in a cron script. But 'top' returns  values as string , like "520M" or "1420M+" and i need values as 520 or 520000  to be able to use it as:  precodeIf ( $MEMO &gt;= 4000 ) ; then do something; fi /code/pre  Any suggestions ? TIA 