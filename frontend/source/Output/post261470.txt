Terminal error with hdiutil <body> I am trying to make an iso for VMware Fusion using snowyhack.sh with an image I made using my OS X 10.6 install disk. When using the command codesudo ./snowyhack.sh Mac\ OS\ X\ Install\ DVD.dmg/code I get this error codehdiutil: attach: extra image argument "OS" - "Mac" already specified/code  The content of snowyhack.sh:  precode#!/bin/bash # Copyright Dave Parsons 2016 #set -x set -e set -E  # Check we have an input parameter if [ "$#" -ne 1 ] then   echo "Usage: snowyhack.sh path_to_dmg"   exit 1 fi  # The first argument is the path to the .app bundle (the input of the # executable). inputDMG="$1" outputISO="${inputDMG%.*}.iso"  if [ ! -f "$inputDMG" ] then   echo "$inputDMG not found."   exit 2 fi  echo Converting $inputDMG to $outputISO  # Clean up any shadow files rm -f $inputDMG.shadow  # Attach DMG with shadow file  hdiutil attach $inputDMG -shadow -nobrowse  # Enable installer to boot touch /Volumes/Mac\ OS\ X\ Install\ DVD/System/Library/CoreServices/ServerVersion.plist  # Add startup script to fix the target VM cp rc.cdrom.local /Volumes/Mac\ OS\ X\ Install\ DVD/private/etc/ chmod +x /Volumes/Mac\ OS\ X\ Install\ DVD/private/etc/rc.cdrom.local  # Detach the mounted installer DMG hdiutil detach /Volumes/Mac\ OS\ X\ Install\ DVD/  # Convert to ISO using the shadow file hdiutil convert -format UDTO -o $outputISO $inputDMG -shadow mv -fv $outputISO.cdr $outputISO  # Clean up any shadow files rm -f $inputDMG.shadow /code/pre  <comment327401> Without knowing what's in snowyhack.sh, we can only guess. But my guess is that snowyhack.sh doesn't properly double-quote variable references, leading to word-splitting problems. <comment327404> That doesn't work, it gives me the same error. (BTW the user is jordanbaron, not jordan) <comment327405> @JordanBaron ;-). I haven't been able to hack me into your Mac that fast to get your proper user name and the real path to the dmg. But: I will start my Sierra VM and test the shell script. <comment327412> I renamed my DMG file to OSX.dmg and when using the same command I get this: 'expected CRC32 $9CBFCD75 /dev/disk6 1 DVD cp: rc.cdrom.local: No such file or directory' <comment327414> @JordanBaron If you have downloaded the proper zip file, it also contains the file rc.cdrom.local which should reside in the same folder as the shell script *snowyhack.sh*. <comment327416> Oh ok. I forgot to drag it to my desktop. Thanks! <comment327419> Now when I boot from the ISO it takes 2 minutes and instead of booting it shows a prohibit sign instead of the apple logo appears <comment327461> This would work fine with spaces in the names IF you double-quote all variable references, like `rm -f "$inputDMG.shadow"` and `hdiutil attach "$inputDMG" -shadow -nobrowse`. [Shellcheck.net](http://www.shellcheck.net/) is a good tool for finding basic problems like this in shell scripts. Oh, and please don't mess with IFS unless you know what you're doing -- it can cause some really weird problems.