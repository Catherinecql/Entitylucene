How do I create a desktop shortcut for VNC? <body> I connect to my Raspberry Pi with VNC from my MacBook, however to do that every time I have to open the Finder, then select Go from the menu, then Connect to server. I am looking forward for having a shortcut (or an alias) on the desktop so I have to take minimal steps.  This is the address I use for VNC connection: codevnc://raspberrypi.local:5901/code.  <answer250763> ol liOpen a VNC session to your Raspberry Pi (e.g. Safari or run codeopen vnc://&lt;host&gt;:&lt;port&gt;/code)./li liGrab the small icon to the left of the host name with your pointer/li liDrag and drop the icon to the desktop/li /ol  It will create a code.vncloc/code shortcut, which then you can double-click to open the connection.  img src="https://i.stack.imgur.com/117TR.png" alt="enter image description here" 