Get posix path of .webloc in applescript droplet <body> If a .webloc is dropped onto the following script, the dialog won't be displayed; instead the url of the webloc is opened in the default browser.  precodeon open dropped_webloc     display dialog "hey" end open /code/pre  If a .webloc is dropped onto the following script, the dialog is shown, but how can I access the posix path of the dropped .webloc file?  precodeon open location dropped_webloc     display dialog "hey" end open /code/pre 