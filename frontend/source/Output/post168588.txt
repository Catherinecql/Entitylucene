How can I write a launchd service that will actually stop the service? <body> So I have a plist to keep monit running:  precode&lt;?xml version="1.0" encoding="UTF-8"?&gt; &lt;!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"&gt; &lt;plist version="1.0"&gt; &lt;dict&gt;     &lt;key&gt;Label&lt;/key&gt;     &lt;string&gt;com.monit.service&lt;/string&gt;     &lt;key&gt;KeepAlive&lt;/key&gt;     &lt;true/&gt;     &lt;key&gt;ProgramArguments&lt;/key&gt;     &lt;array&gt;         &lt;string&gt;/usr/local/Cellar/monit/5.10/bin/monit&lt;/string&gt;         &lt;string&gt;-d 60&lt;/string&gt;     &lt;/array&gt;     &lt;key&gt;RunAtLoad&lt;/key&gt;     &lt;true/&gt;     &lt;key&gt;StandardOutPath&lt;/key&gt;     &lt;string&gt;/var/log/monit.log&lt;/string&gt; &lt;/dict&gt; &lt;/plist&gt; /code/pre  I can do codelaunchctl load /Library/LaunchDaemons/com.monit.service.plist/code to start the service, but codelaunchctl unload /Library/LaunchDaemons/com.monit.service.plist/code will not stop it. I can see that the process keeps running no matter what I do.  The process shows up like this(note how the binary path doesn't show up, only the last argument):  output of ps aux:  precoderoot            14656   0.0  0.0  2482248   1656   ??  S     6:55PM   0:00.09 -d 60 /code/pre  How can I write the launchd plist so that it will actually stop the freaking process whenever I want it to stop?  <answer168602> I suspect since it is running from code/Library/LaunchDaemons//code  and not the user code~/Library/LaunchAgents/code you need to use codesudo/code to issue the command with permission.  codesudo launchctl unload /Library/LaunchDaemons/com.monit.service.plist/code  <answer168638> Get rid of the code-d 60/code arguments and use the codelaunchd/code key codeStartInterval/code instead. Also make sure the process does not daemonize itself. According to the codemonit/code man page this is accomplished by using the code-I/code option.  <comment197916> I'm running these commands as root, so I don't think that's the issue. <comment198597> For the record, my monitrc file only had this:`set daemon 60` and I needed this: `set init set daemon 60`. So essentially, I was daemonizing monit.