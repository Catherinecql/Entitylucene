Mount Error when attempting to mount a network server <body> I've been attempting to mount a network server on my computer. I can do it through Finder, but this is inconvenient since I can only connect with one username at a time. I read that mounting via Terminal would allow me to connect with multiple usernames at the same time. I found this post: How can I mount an SMB share from the command line? where the command to mount is  precodemount_smbfs //user@SERVER/folder ./mntpoint /code/pre  but whether I do that or codemount -t smbfs/code instead of codemount_smbfs/code I get the same error:  precodemount_smbfs: mount error: [mountpoint path]: Input/output error /code/pre  Someone even mentioned getting the same error in that post I linked to above, but no one answered him.  Edit: For the sake of being complete, here's my actual input and output (with the username and password replaced). I even created a new directory /Volumes/testShare to test it with.  precodeJamess-MacBook-Pro:Volumes magavendon$ mount -t smbfs //[user]:[password]@172.25.206.80 /Volumes/testShare mount_smbfs: mount error: /Volumes/testShare: Input/output error /code/pre  And the mount_smbfs command:  precodeJamess-MacBook-Pro:Volumes magavendon$ mount_smbfs //[user]:[password]@172.25.206.80 /Volumes/testShare mount_smbfs: mount error: /Volumes/testShare: Input/output error /code/pre  <answer129760> You probably need to do codemkdir ./mntpoint/code first.  For example:  precodemkdir /Volumes/home mount_smbfs //tom:tomspassword@fileserver/home /Volumes/home /code/pre  <answer129784> So, you want to go the hard way, here you go :  precodemkdir /Volumes/[share] | mount_smbfs //[user]:[password]@[server]/[share] /Volumes/[share] /code/pre  BUT:  You can connect multiple times to the same serverbr with different users via the kbd⌘ cmd/kbd + kbdK/kbd Box in Finder.  Basically: codesmb://user@server/code (or SMB1 = codecifs://user@server/code).  It automatically adds the a number to every duplicate share that is connected,br so if you have "Data" twice on the server, but for different users, or for same users, but with different priviliges, then Mac creates /Volumes/Data first, then on the second connection creates /Volumes/Data-1.  But this is not visible to the User, you see both as "Data" Sharepoints in Finder.  <comment151860> I did already create the directory. I even tried a different directory ./test just in case. <comment151893> you did change the ./mntpoint to a valid path, right ? <comment151896> yes, but try removing the dot. The Dot is translated as "Current Directory". The / is the root directory. So basics here: cd /Volumes = Volumes folder in root of HDD; cd . = Current directory; cd ./Desktop = Desktop folder in your home folder; cd ~ = your home folder;  Now if you try to mount a Volumes at ./test it creates the mountpoint in the current dir under currentDir/test. <comment151973> Thank you. Sorry I guess my previous statement wasn't very clear. I didn't make a directory called ./test just test. I used ./test when passing the directory I wanted to put the mount point in for the mount command. <comment151975> I did figure out how to connect this way through Finder.  But when I connect with a different user the data that should be in that user's directory doesn't show up. I was hoping that if I could mount through terminal that might solve my problem. Furthermore, even if I did get the user's data to show up in Finder when logging in with the correct user I still wouldn't be able to log in with multiple users _at the same time_, which is what I want to do. <comment151976> Just to clarify. My problem input/output error isn't that I don't have the directory correctly named or existing. I know how to do that.