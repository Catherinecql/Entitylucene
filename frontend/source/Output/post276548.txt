List all keys added to keychain using `security add-generic-password` <body> Is there a command in codesecurity/code that lists the names of strongall/strong keys that have been added to the keychain through codeadd-generic-password/code?  I've tried, but it codefind-generic-password/code only ever returns one result as far as I know.  <answer276562> This did it for me  precodesecurity dump-keychain | grep 0x00000007 | awk -F= '{print $2}' /code/pre 