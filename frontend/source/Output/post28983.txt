Getting the longitude and latitude from Google Maps <body> I got a friend who sent me his location via WhatsApp. I am also having the navigation software sygic installed, and it accepts the longitude and latitude to specifiy the location.  For it can I pull the longitude and latitude from google maps and sent them in Sygic?  <answer29045> Yes, you can get your coordinates via Maps app. To do that:  ol liTap on your current location or drop a pin to a location of your choosing and then tap./li liTap on the arrow within the blue circle/li liTap on share location, choose email/li liSend it to yourself./li liOn the email, there should be a link and a vcard. Both of them include coordinate information. However to get the actual coordinates you'll need to copy the link in the email and paste it into, e.g. Notes, to then select just the coordinates./li /ol  And you can input the coordinates into the Sygic app.  <answer29054> Copy and paste the following code into your browser address bar:  codejavascript:void(prompt(”,gApplication.getMap().getCenter()));/code  <answer35385> Go a random webpage using Safari (eme.g. http://google.com/em).  Go the address bar, and type codejavascript:navigator.geolocation.getCurrentPosition(function(p){var c=p.coords;document.write(c.latitude+','+c.longitude);});/code (if you see any popups, select OK).  Because Safari is not user-friendly, it doesn't allow you to type certain characters in the address bar. To be able to type codejavascript:navigator.geolocation.getCurrentPosition(function(p){var c=p.coords;document.write(c.latitude+','+c.longitude);});/code in the address bar, you have to:  ol liOpen your Messaging app, go to "Write a Message", type codejavascript:navigator.geolocation.getCurrentPosition(function(p){var c=p.coords;document.write(c.latitude+','+c.longitude);});/code/li liDouble tap  Select All  Copy/li liNow go back to Safari app and paste what you have copied into the address bar./li /ol  <comment33375> While the question doesn't make it as obvious as it could, the question is about the Google Maps app on iOS, not about the Google Maps website. Consequently, there is no browser address bar, and bookmark lets aren't an option. But this is a good answer to a different problem. <comment40290> See my solution instead