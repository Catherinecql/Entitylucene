GUI Firefox, Safari and other apps freeze after 10.11.3 update <body> Some apps seem to freeze when they were started at login. The apps are still responsive, but the GUI does not update. Did anyone else experience this problem? What can be done to solve it?  <answer231962> I'd start with the standard 'troubleshooting toolkit'..  ul liFix permissions, Verify &amp; if necessary Repair disk, using Disk Utility./li liReset Home folder permissions &amp; ACLs using OnyX  - Maintenance tab/permissions, tick the box at the top then Execute. (Make sure you get the right version for your OS.)/li liReset the SMC (instructions depend on Mac model)/li liReset the NVRAM  ol liAt the boot chimes, hold kbd Cmd ⌘ /kbd kbd Opt ⌥ /kbd kbd P /kbd kbd R /kbd /li liKeep holding all 4 keys until you hear the chimes again, then release/li /ol/li liStart in Safe Mode   ol liStart or restart your Mac. /li liImmediately after you hear the startup sound, press and hold the Shift key./li liRelease the Shift key when you see the Apple logo appear on the screen./li liLeave a few minutes after login for caches to rebuild, then reboot normally./li /ol/li liUpdate to 10.9.5 using the 10.9.5 combo update, not the delta from App Store/li /ul 