Is there a fast way to switch System time zone from the command line? <body> I usually do it through the System Preference panel for Date &amp; Time, but I'd like a quicker way to do this if there is one.  <answer117995> Use the following Terminal command:  precodesudo systemsetup -settimezone timezone /code/pre  For a list of valid codetimezone/code values, use codesudo systemsetup -listtimezones/code.  To get the current timezone, use:  precodesudo systemsetup -gettimezone /code/pre  <answer273346> Additionally:  strongPrinting a random timezone/strong:  precoderuby -e "puts `sudo systemsetup -listtimezones`.lines.sample.strip" /code/pre  strongSetting a random timezone/strong:  precodesudo systemsetup -settimezone (ruby -e "puts `sudo systemsetup -listtimezones`.lines.sample.strip") /code/pre  strongFinding a timezone/strong (e.g. containing "Los" in this case):  precodesudo printf ''; sudo systemsetup -listtimezones | grep Los /code/pre  <comment170239> And just for the record, you can also *get* the current timezone by doing `sudo systemsetup -gettimezone`. <comment205886> is there a way to get the timezone without root? <comment263606> How can I update NTP source from CLI? <comment312386> Sadly, this isn't working for me. It seems to change it (says `Set TimeZone: America/New_York` and returns exit code 0) but then `sudo systemsetup -gettimezone` returns the previous value (`Time Zone: America/Los_Angeles`).