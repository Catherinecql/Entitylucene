What is the hash to a terminal command? <body> I entered a Terminal command into TextEdit, but i don't know the hash to Terminal, so I can't change it. I tried changing it into .bash and .rtf, but they don't work. Help Plz???   <answer259460> When using TextEdit to write shell scripts or similar,   ul limake sure that TextEdit is in plan text (and not in rich text) mode (check the format menu)/li liuse code#!/bin/bash/code as the first line/li lirun codechmod +x /path/to/file/code once after saving it/li liuse code/path/to/file/code (or, if it is in the current directory, code./file/code) to execute it/li /ul 