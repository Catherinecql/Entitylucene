How can I add autocorrect shortcuts programmatically? <body> In strongSystem Preferences  Keyboard  Text/strong, I can add shortcuts that will be available to me globally via autocorrect.  img src="https://i.stack.imgur.com/uLWFk.png" alt="Keyboard section in System Preferences"  Lets say I had 100 of these shortcuts and they were on two Macs, rather than adding/removing each individual item by hand, is there a way to maintain a single code.plist/code file that could manage these shortcuts?  <answer114427> You can copy the settings to another Mac by copying code~/Library/Preferences/.GlobalPreferences.plist/code.  Note that preferences are cached on 10.9, so if you replace a plist or edit it directly, quitting and reopening applications won't apply the changes. You can apply the changes by running codedefaults read -g/code, by running codekillall cfprefsd/code, or by logging out and back in. Changes made with defaults are still applied if you just quit and reopen an application.  Print the current settings:  precodedefaults read -g NSUserDictionaryReplacementItems /code/pre  Add one setting:  precodedefaults write -g NSUserDictionaryReplacementItems -array-add '{on=1;replace=cmd;with="⌘";}' /code/pre  Replace all settings:  precodedefaults write -g NSUserDictionaryReplacementItems '( {on=1;replace=cmd;with="⌘";}, {on=1;replace=opt;with="⌥";} )' /code/pre  Print the current settings as XML:  precodedefaults read -g NSUserDictionaryReplacementItems | plutil -convert xml1 - -o - /code/pre  Replace all settings:  precodeamp() { local x=${1//&amp;/&amp;amp; }; x=${x//&lt;/&amp;lt; }; printf %s "${x//&gt;/&amp;gt; }"; } out='&lt;array&gt;' while IFS= read -r l; do   out+="&lt;dict&gt;&lt;key&gt;on&lt;/key&gt;&lt;integer&gt;1&lt;/integer&gt;&lt;key&gt;replace&lt;/key&gt;&lt;string&gt;$(amp "${l%% *}")&lt;/string&gt;&lt;key&gt;with&lt;/key&gt;&lt;string&gt;$(amp "${l#* }")&lt;/string&gt;&lt;/dict&gt;" done &lt;&lt;&lt; 'cmd ⌘ opt ⌥' defaults write -g NSUserDictionaryReplacementItems "$out&lt;/array&gt;" /code/pre  Replace codeNSUserDictionaryReplacementItems/code with codeNSUserReplacementItems/code in 10.8 and earlier.  <comment134052> in Lion and Mountain Lion is a hidden file .GlobalPreferences.plist in the local Preferences folder. Does this not exist in Mavericks?  For more information to copy the file under Lion see http://apple.stackexchange.com/questions/57960/how-can-i-export-text-substitutions-from-lion-for-import-into-mountain-lion