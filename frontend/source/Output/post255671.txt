ERROR! MySQL server PID file could not be found! <body> I have reinstalled macOS Sierra on my machine. After installing Oracle MySQL Community Server 5.7.15 (mysql-5.7.15-osx10.11) I tried to stop the MySQL server from the console. I got back the following error "ERROR! MySQL server PID file could not be found!".  I have done some research on my own but it did not manage to find a solution on my problem.  Any suggestions?  <answer255741> Starting and stopping the MySQL server via Preferences Pane/launchd and on the other hand via codesudo /usr/local/mysql/support-files/mysql.server start/stop/code are not really compatible.  After choosing "Launchd Support" in the MySQL installer the launch daemon emcom.oracle.oss.mysql.mysqld/em will be created and the MySQL preferences pane uses the launchd mechanism to start and stop mysql. If you enable "Automatically Start MySQL Server on startup", mysqld is enabled automatically after starting your Mac.  The pid file's name created by the launch daemon is not compatible with the one created and expected by the mysql-server script.  The launch daemon's pid file name created while running is emmysql.local.pid/em, the script expects a name based on your Mac's hostname, .local or computer name though.  So either use the MySQL pref pane/launchctl or the script to start and stop mysqld.  hr  To some extent you can make both methods compatible by modifying the file emcom.oracle.oss.mysql.mysqld/em though:  After stopping mysql replace the array  precode&lt;array&gt;     &lt;string&gt;/usr/local/mysql/bin/mysqld&lt;/string&gt;     &lt;string&gt;--user=_mysql&lt;/string&gt;     &lt;string&gt;--basedir=/usr/local/mysql&lt;/string&gt;     &lt;string&gt;--datadir=/usr/local/mysql/data&lt;/string&gt;     &lt;string&gt;--plugin-dir=/usr/local/mysql/lib/plugin&lt;/string&gt;     &lt;string&gt;--log-error=/usr/local/mysql/data/mysql.local.err&lt;/string&gt;     &lt;string&gt;--pid-file=/usr/local/mysql/data/mysql.local.pid&lt;/string&gt; &lt;/array&gt; /code/pre  by  precode&lt;array&gt;     &lt;string&gt;/usr/local/mysql/bin/mysqld&lt;/string&gt;     &lt;string&gt;--user=_mysql&lt;/string&gt;     &lt;string&gt;--basedir=/usr/local/mysql&lt;/string&gt;     &lt;string&gt;--datadir=/usr/local/mysql/data&lt;/string&gt;     &lt;string&gt;--plugin-dir=/usr/local/mysql/lib/plugin&lt;/string&gt;     &lt;string&gt;--log-error=/usr/local/mysql/data/$name.err&lt;/string&gt;     &lt;string&gt;--pid-file=/usr/local/mysql/data/$name.pid&lt;/string&gt; &lt;/array&gt; /code/pre  with $name: either HostName (e.g. host.example.com), LocalHostName (e.g. host.local) or ComputerName (e.g. host). Probably the best is to use a hostname. You may get a hostname with hostname with codescutil --get HostName/code or set it with codesudo scutil --set HostName your_hostname/code.   You can also get the actually used mysql-server script pid's name - after stopping the launch daemon and starting mysqld with codemysql-server start/code-  by checking mysql's data directory with codesudo ls /usr/local/mysql/data/code. Use the name found there to adjust the plist's pid name.  You may have to convert the launch daemon file to xml to edit it. The app TextWrangler opens it as xml file immediately.  Reload the plist with launchctl afterwards.  hr  Now should be able to start and stop with the MySQL pref pane and the mysql.server script.  If you have enabled "Automatically Start MySQL Server on Startup", you strongcan't stop/strong the server – even after modifying the plist – with the script because launchd overrides any emstop/em command after a few seconds and launches mysqld again.  <comment318445> You reinstalled Sierra **after** installing MySQL... **or** You reinstalled Sierra **and** after installing MySQL.... Also: which MySQL installer did you use - the Oracle MySQL Community 5.7.15 pkg installer or the macports package or both? <comment318450> I did a clean sierra install, after that i configured my built in apache and then I installed MySQL using the Oracle MySQL Community 5.7.15 pkg. I am also using homebrew. <comment318457> How did you start MySql? <comment318459> just by clicking the button on the settings menu.