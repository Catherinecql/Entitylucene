How to download images from an xml feed using terminal? <body> How can I download png images from an xml feed that looks the following way?  precode&lt;ListBucketResult xmlns="http://url1.com"&gt;   &lt;Name&gt;imagesToUse&lt;/Name&gt; &lt;Prefix/&gt; &lt;Marker/&gt; &lt;IsTruncated&gt;false&lt;/IsTruncated&gt; &lt;Contents&gt;   &lt;ImageStuff&gt;imagetodownload.png&lt;/ImageStuff&gt;   &lt;Size&gt;17293&lt;/Size&gt; &lt;/Contents&gt; /code/pre  I was taking a look at this question on here about the same topic, except that I don't need to rename them, just download them.  Being new to shell scripts, I'm not quite sure how to do this (although I can definitely do this in 2 minutes in objective-c).  I have tried creating the following script and running codebash myscript.sh/code  precode#!/bin/bash URL='https://theurlwiththexml.com' i=0 for u in $(curl -s "$URL" |            grep png |             sed -E 's/.*&lt;ImageStuff&gt;(.+)&lt;\/ImageStuff&gt;/\1/'); do     curl -s "$u" -o $i.png     (( i++ )) done /code/pre  But nothing happens. It just prints another line without errors.  <answer139702>   After codecurl http://theurlwiththexml.com/code:  pre class="lang-none prettyprint-override"codesed -n 's,.*&lt;ImageStuff&gt;\(.*\.png\)&lt;.*,\1,p'|xargs -I, curl http://url1.com/, -o , /code/pre  codesed -n/code disables printing, code\(/code and code\)/code are a portable way to specify groups, and codep/code prints the pattern space. codexargs -I/code specifies a replacement string and codecurl -o/code specifies an output file.  Or using codexmlstarlet/code, codeparallel/code, and codewget/code:  pre class="lang-none prettyprint-override"codexml sel -t -v //ImageStuff|parallel wget http://url1.com/{} /code/pre  codesel/code is an abbreviation for codeselect/code, code-t -v/code uses the value template, and code//ImageStuff/code is an XPath expression that matches all codeImageStuff/code nodes. 