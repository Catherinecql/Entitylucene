How to unload kernel modules <body> When I run codekextstat/code, it gives me a list of the currently loaded kernel modules. How to I go about using the information gathered from this command to unload a kernel module?  <answer60497> You can unload kernel modules with codekextunload/code either specifying the whole path to the kernel extension as a parameter (they reside in code/System/Library/Extensions//code) or by specifying the bundle identifier displayed by codekextstat/code, e.g. if you want to unload Fuse for OS X:  precodesudo kextunload -b com.github.osxfuse.filesystems.osxfusefs /code/pre  You can then use codekextload/code for loading extension in the same way.  strongBonus hint:/strong If you only want to see 3rd party kernel extensions that aren’t from Apple you can use inverse grep:  precodekextstat | grep -v com.apple /code/pre 