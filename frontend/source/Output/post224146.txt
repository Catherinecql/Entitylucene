AppleScript to Like a SoundCloud Track using System Events <body> I'd like to create an AppleScript to like a SoundCloud track.  Since SoundCloud supports keyboard shortcuts, I tried to use System Events. (l "ell" is the shortcut.)  This is what I have so far, but it doesn't work:  precodetell application "Google Chrome Canary"     repeat with currentWindow in every window         set allTabs to currentWindow's tabs         repeat with currentTab in allTabs             if URL of currentTab contains "soundcloud" then                 tell application "System Events"                     keystroke "l"                 end tell             end if         end repeat     end repeat end tell /code/pre 