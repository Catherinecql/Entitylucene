Terminal returns "command not found" for most commands <body> I modified .bash_profile to look like this  precode# added by Anaconda3 4.4.0 installer export PATH="/anaconda/bin:$PATH" export PATH=”$HOME/bin:$PATH” export PATH="$HOME/adb-fastboot/platform-tools:$PATH" /code/pre  after that almost every command returns -bash: command: command not found.  How do I reset it?  <comment373315> What text editor did you use to modify .bash_profile? <comment373326> i used textedit